# =======================================================
# === FARMALUX E-COMMERCE - Environment Configuration ===
# =======================================================
# Este archivo es el punto de partida para el ambiente de pruebas.
# Copiar como .env antes de ejecutar cualquier script.
# .env.example = .env (siempre)

# ===== Base de Datos (SQLite Local) =====
# El sistema usa SQLite por defecto. Los archivos .db se crean en DB_PATH.
DB_TYPE=sqlite
DB_PATH=./data/database

# ===== Uploads =====
UPLOAD_PATH=./data/uploads

# ===== Servidor Backend (FastAPI) =====
BACKEND_HOST=localhost
BACKEND_PORT=8000

# ===== Servidor Frontend (Vite) =====
FRONTEND_HOST=localhost
FRONTEND_PORT=5173

# ===== Credenciales Admin Default =====
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin2024

# ===== JWT Security =====
# Generar nuevo con: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=T4h2yj-bq-YXBIHinixY7YHC-3DG714DqiR-IQxVNNQ
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ===== Python Version (Para setup.py) =====
PYTHON_VERSION=3.11

# ===== Servicios Externos (Opcional - Desarrollo) =====
# SMTP para recuperación de contraseña
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=no-reply@example.com
SMTP_PASSWORD=changeme
SMTP_FROM_EMAIL=no-reply@example.com

# hCaptcha (dejar vacío para dev, el backend omite validación)
HCAPTCHA_SECRET_KEY=

# ===== Feature Flags =====
DEBUG=true
ENABLE_DOCS=true